# Dependências do empacotamento serverless (AWS Lambda + Function URL).
# Diferenças em relação a requirements.txt:
#  - sem uvicorn (o Lambda invoca via Mangum, não por um servidor HTTP próprio)
#  - sem anthropic (produção usa OpenAI; reduz o tamanho do pacote)
#  - com mangum (adapta o FastAPI ao formato de evento do Lambda)
#  - boto3 NÃO entra aqui: já vem pré-instalado no runtime do Lambda
fastapi>=0.115
jinja2>=3.1
httpx>=0.27
openai>=2.0
python-dotenv>=1.0
python-multipart>=0.0.12
openpyxl>=3.1
markdown>=3.7
mangum>=0.17
